export class Body {
  sex: string;
  length:number;
  weight:number;
  age:number;

  constructor(sex, length, weight, age) {
    this.sex = sex;
    this.length = length;
    this.weight = weight;
    this.age = age;
  }
}
